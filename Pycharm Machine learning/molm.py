# Import the venv module
import venv

# Specify the directory where you want to create the virtual environment
env_dir = "/path/to/non_lightweight_env"

# Create the virtual environment
venv.create(env_dir)